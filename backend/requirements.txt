# Dependencias del backend — Plataforma Web de Bingo
# Instalar con:  .venv\Scripts\python.exe -m pip install -r requirements.txt

# --- Framework web y servidor ---
# uvicorn[standard] trae el soporte de WebSockets que necesita la balotera.
fastapi
uvicorn[standard]

# --- Base de datos ---
# aiosqlite es Python puro: sin riesgo de wheels faltantes en Python 3.14.
# Para pasar a PostgreSQL en la Fase 2 basta con agregar asyncpg y cambiar
# DATABASE_URL — ver README.md de esta carpeta.
sqlalchemy[asyncio]
aiosqlite
alembic

# --- Configuración y validación ---
pydantic-settings

# --- Pruebas ---
pytest
pytest-asyncio
httpx
