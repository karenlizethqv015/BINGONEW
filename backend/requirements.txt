# Dependencias del backend — Plataforma Web de Bingo
# Instalar con:  .venv\Scripts\python.exe -m pip install -r requirements.txt

# --- Framework web y servidor ---
# uvicorn[standard] trae el soporte de WebSockets que necesita la balotera.
fastapi
uvicorn[standard]

# --- Base de datos ---
# Van los dos motores a la vez, y el que se use lo decide DATABASE_URL:
#
#   aiosqlite  desarrollo y pruebas. Python puro, no exige levantar nada.
#   asyncpg    la demo desplegada y la instalación final en la sala.
#
# Comprobado que asyncpg 0.31 trae wheel tanto para la 3.13 de la imagen de
# Docker como para la 3.14 del entorno de desarrollo, así que no compila nada en
# ninguno de los dos. Detalle en README.md de esta carpeta.
sqlalchemy[asyncio]
aiosqlite
asyncpg
alembic

# --- Configuración y validación ---
pydantic-settings

# --- Pruebas ---
pytest
pytest-asyncio
httpx
